<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Connections - Framefox Connect" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", type: "module" %>
    <%= javascript_include_tag 'https://kit.fontawesome.com/84f8d5c51f.js', crossorigin: 'anonymous' %>
  </head>
  <body class="h-screen overflow-hidden">
    <!-- Impersonation Banner -->
    <% if impersonating? && impersonated_customer %>
      <div class="bg-yellow-500 border-b-2 border-yellow-600 px-4 py-3 text-white z-50">
        <div class="container mx-auto flex items-center justify-between">
          <div class="flex items-center space-x-3">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
            <span class="font-semibold">
              Admin Impersonation Active: Viewing as <%= impersonated_customer.full_name %>
            </span>
          </div>
          <%= button_to stop_impersonating_admin_shopify_customers_path, 
              { method: :delete },
              { class: "bg-yellow-600 hover:bg-yellow-700 text-white px-4 py-1.5 rounded-md text-sm font-medium transition-colors inline-flex items-center space-x-2" } do %>
            <span>Stop Impersonating</span>
          <% end %>
        </div>
      </div>
    <% end %>
    <div class="flex h-full">
      <!-- Sidebar Navigation -->
      <%= render 'shared/sidebar' %>
      <!-- Main Content Area -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top bar for mobile -->
        <div class="lg:hidden bg-white border-b border-slate-200 px-4 py-3">
          <h1 class="text-lg font-semibold text-slate-900">
            <%= content_for(:title) || "Framefox Connect" %>
          </h1>
        </div>
        <!-- Main Content -->
        <main class="flex-1 overflow-y-auto">
          <div class="container mx-auto px-6 py-8 lg:pl-8">
            <% if notice %>
              <div data-flash-notice="<%= notice %>" style="display: none;"></div>
            <% end %>
            <% if alert %>
              <div data-flash-alert="<%= alert %>" style="display: none;"></div>
            <% end %>
            <% if flash[:warning] %>
              <div data-flash-warning="<%= flash[:warning] %>" style="display: none;"></div>
            <% end %>
            <% if flash[:info] %>
              <div data-flash-info="<%= flash[:info] %>" style="display: none;"></div>
            <% end %>
            <%= yield %>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
