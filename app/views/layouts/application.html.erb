<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Connections - Framefox Connect" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'https://kit.fontawesome.com/84f8d5c51f.js', crossorigin: 'anonymous' %>
  </head>
  <body class="h-screen overflow-hidden">
    <div class="flex h-full">
      <!-- Sidebar Navigation -->
      <%= react_component("Sidebar", { 
        currentPath: request.path,
        logo_path: asset_path('pro-logo.png')
      }) %>
      <!-- Main Content Area -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top bar for mobile -->
        <div class="lg:hidden bg-white border-b border-slate-200 px-4 py-3">
          <h1 class="text-lg font-semibold text-slate-900">
            <%= content_for(:title) || "Framefox Connect" %>
          </h1>
        </div>
        <!-- Main Content -->
        <main class="flex-1 overflow-y-auto">
          <div class="container mx-auto px-6 py-8 lg:pl-8">
            <% if notice %>
              <div data-flash-notice="<%= notice %>" style="display: none;"></div>
            <% end %>
            <% if alert %>
              <div data-flash-alert="<%= alert %>" style="display: none;"></div>
            <% end %>
            <% if flash[:warning] %>
              <div data-flash-warning="<%= flash[:warning] %>" style="display: none;"></div>
            <% end %>
            <% if flash[:info] %>
              <div data-flash-info="<%= flash[:info] %>" style="display: none;"></div>
            <% end %>
            <%= yield %>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
