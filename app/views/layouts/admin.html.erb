<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Framefox Connect - Admin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", type: "module" %>
    <%= javascript_include_tag 'https://kit.fontawesome.com/84f8d5c51f.js', crossorigin: 'anonymous' %>
  </head>
  <body class="bg-slate-50">
    <!-- Admin Header -->
    <header class="bg-white border-b border-slate-200 ">
      <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <%= link_to admin_root_path, class: "text-xl font-semibold text-slate-900" do %>
              Framefox Connect Admin
            <% end %>
          </div>
          <nav class="flex items-center space-x-4">
            <%= link_to "Dashboard", admin_root_path, class: "text-sm font-medium transition-colors hover:text-slate-900 text-slate-500" %>
            <!-- Shopify Dropdown -->
            <div data-dropdown class="relative">
              <button 
                data-dropdown-trigger 
                class="text-sm font-medium transition-colors hover:text-slate-900 text-slate-500 flex items-center gap-1"
              >
                Shopify Production
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>
              <div 
                data-dropdown-menu 
                class="hidden absolute left-0 mt-2 w-48 bg-white rounded-md shadow-lg border border-slate-200 py-1 z-50"
              >
                <%= link_to "Customers", admin_shopify_customers_path, class: "block px-4 py-2 text-sm text-slate-700 hover:bg-slate-100 transition-colors", role: "menuitem" %>
                <%= link_to "Companies", admin_companies_path, class: "block px-4 py-2 text-sm text-slate-700 hover:bg-slate-100 transition-colors", role: "menuitem" %>
              </div>
            </div>
            <%= link_to "Connected Stores", admin_stores_path, class: "text-sm font-medium transition-colors hover:text-slate-900 text-slate-500" %>
            <%= link_to "Users", admin_users_path, class: "text-sm font-medium transition-colors hover:text-slate-900 text-slate-500" %>
            <% if current_user %>
              <%= button_to "Logout", destroy_user_session_path, method: :delete, 
                  class: "text-sm font-medium transition-colors hover:text-slate-900 text-slate-500 bg-transparent border-0 cursor-pointer" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>
    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
      <% if notice %>
        <div data-flash-notice="<%= notice %>" style="display: none;"></div>
      <% end %>
      <% if alert %>
        <div data-flash-alert="<%= alert %>" style="display: none;"></div>
      <% end %>
      <% if flash[:warning] %>
        <div data-flash-warning="<%= flash[:warning] %>" style="display: none;"></div>
      <% end %>
      <% if flash[:info] %>
        <div data-flash-info="<%= flash[:info] %>" style="display: none;"></div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
