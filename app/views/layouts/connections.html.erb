<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Connections - Framefox Connect" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50">
    <!-- Connections Header -->
    <header class="bg-white border-b border-slate-200 shadow-sm">
      <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <%= link_to root_path, class: "text-xl font-semibold text-slate-900" do %>
              Framefox Connect
            <% end %>
            <nav class="flex items-center space-x-1">
              <%= link_to root_path, class: "text-sm text-slate-500 hover:text-slate-700" do %>
                Home
              <% end %>
              <span class="text-slate-400">/</span>
              <%= link_to connections_root_path, class: "text-sm font-medium text-slate-900" do %>
                Connections
              <% end %>
            </nav>
          </div>
          
          <nav class="flex items-center space-x-4">
            <%= link_to connections_root_path, class: "text-sm font-medium transition-colors hover:text-slate-900 #{'text-slate-900' if request.path == connections_root_path} #{'text-slate-500' unless request.path == connections_root_path}" do %>
              Dashboard
            <% end %>
            <%= link_to connections_stores_path, class: "text-sm font-medium transition-colors hover:text-slate-900 #{'text-slate-900' if request.path.start_with?(connections_stores_path)} #{'text-slate-500' unless request.path.start_with?(connections_stores_path)}" do %>
              My Stores
            <% end %>
            <%= link_to admin_root_path, class: "text-sm font-medium transition-colors hover:text-slate-900 text-slate-500" do %>
              Admin
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
      <% if notice %>
        <div class="mb-6 border-green-200 bg-green-50 text-green-900 relative w-full rounded-lg border p-4">
          <%= notice %>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="mb-6 border-red-200 bg-red-50 text-red-900 relative w-full rounded-lg border p-4">
          <%= alert %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
